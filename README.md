# dotfiles

My dotfiles

## Fedora Quickstart

```bash
sudo dnf install git -y && git clone https://github.com/mmoershe/dotfiles ~/dotfiles && chmod +x ~/dotfiles/install_fedora.sh && ~/dotfiles/install_fedora.sh
```

## random notes

### tmux

tmux sessionizer?
