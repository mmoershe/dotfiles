# Focus workspace 1 on startup
exec swaymsg workspace number 1

exec $term
exec $browser
exec mako
exec thunderbird
exec blueman-applet

exec_always bash -c "pkill waybar; waybar"

output * bg $wallpaper_directory fill
